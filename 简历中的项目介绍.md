智能代码审查与发布风险分析平台 （个人项目，独立完成全栈架构设计与全链路开发）2026.2-2026.5
项目规模：知识库累计收录历史事故文档 80+ 篇，覆盖 Java/Python/TypeScript 等 5 个代码仓库，构建代码块向量索引 1.5 万+ 条；累计处理代码审查任务 500+ 次，覆盖业务风险查询与代码漏洞审查两类场景:业务风险（自然语言询问"售票系统是否有超卖风险？"）*和代码漏洞风险（是否有sql注入风险）。
多 Agent 协调与记忆管理：针对审查高延迟痛点，构建多阶段并行流水线（分类→规则/RAG/安全/性能并发→评分→报告）与Agent节点动态裁剪；通过状态共享、熔断及Redis记忆保障高可用，4Agent并发与裁剪节点使延迟降至约串行1/4。
提示词工程：为解决Agent输出不稳定与长文本超限痛点，针对各 Agent 节点设计差异化 systemprompt，结合结构化重试及启发式兜底保障可靠性。通过 分级精简输出上下文至 2000 tokens，控制单次推理输入窗口，避免长上下文超限。
流式输出与异步高可用：基于 SSE 模式实现 Agent 节点结果流式推送与断线重连。异步任务采用本地消息表保证最终一致性，并设置严格的状态机流转规则。经 k6 压测（30 并发/5min），Outbox 投递延迟 P95 < 5s，任务终态到达率 > 99%，突发波峰（80 并发）零死信。
分层架构解耦：为解决单一 Python 服务在高并发下吞吐受限及CPU，GPU利用不充分的问题，引入Java BFF层将CPU 密集的 AST 解析与代码上下文补全前置至 Java 层；Python层保持无状态多实例部署，专注模型服务。该架构使Top-5检索召回率 58%→82%（+24pp）；同时在（k6 200 并发/5min）场景下吞吐量 42→202 req/s（4.8×）。
数据闭环与知识库迭代：为解决系统优化缺乏数据依据的问题，搭建反馈机制，前端点踩携带关键数据落库，为 Prompt迭代、检索策略优化与模型微调提供数据基础。该闭环使迭代周期从纯人工经验驱动的约 2 周缩短至数据可量化追踪，优化方向从定性判断转为定量分析。
