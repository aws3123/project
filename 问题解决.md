# 问题解决记录

分块方式的选型

递归分块有严重问题。

解决方案：



## 1. Kafka 异步消费者未消费消息

**现象**：`POST /api/review/async` 返回 202 + taskId，但任务一直卡在 `PENDING` 状态，未被处理。

**排查过程**：
1. 检查 Kafka topic `ai.review.tasks` 有消息写入（生产者正常）
2. 检查消费者 group，发现 consumer 读的是 `reviewTaskIn-in-0` 而非 `ai.review.tasks`
3. 健康检查显示 `consumerDestinationName='reviewTaskIn-in-0'`，group 是匿名 `anonymous.xxx` 而非 `java-orchestrator`

**根因**：`application.yml` 中 Spring Cloud Stream 消费者 binding 名称拼写错误。

- 函数定义：`spring.cloud.function.definition: reviewTaskIn`
- 自动生成的 binding 名：`reviewTaskIn-in-0`（驼峰函数名 + `-in-0`）
- 配置文件写的：`reviewTask-in-0`（不匹配）

Spring Cloud Stream 未匹配到配置，使用默认值——topic 用 binding 名自身，group 匿名。

**修复**：`backend/src/main/resources/application.yml` 第 18、28 行，`reviewTask-in-0` → `reviewTaskIn-in-0`。

---

## 2. 前端异步/同步直连模式缺少测试覆盖

**现象**：`SubmitPage.test.tsx` 只有 2 个测试，仅覆盖 `auto` 调度模式，缺少直接选择同步/异步模式的测试。

**修复**：
- 导出 `setup.ts` 中的 MSW `server`（`const` → `export const`）
- `SubmitPage.test.tsx` 新增 6 个测试：同步直连、异步直连、必填校验、auto 缺问题校验、API 500 错误、Loading 状态
- 统一使用 `getByRole` 替代 `getByLabelText`

---

## 3. TaskDetailPage 测试耦合 Store 实现细节

**现象**：`TaskDetailPage.test.tsx` 通过 `useTaskStore.getState().upsertTasks()` 直写 Store 模拟状态，耦合实现细节。

**修复**：新建 `TaskDetailPage.integration.test.tsx`，使用 MSW `server.use()` 动态替换 API handler，模拟真实"页面加载 → 请求接口 → 渲染"流程。覆盖 QUEUED、PROCESSING、SUCCESS 三种状态。

**注意事项**：
- 原测试文件使用 `vi.mock`（模块级提升），无法在同一文件中混用 MSW
- 整合测试中 `useTaskPolling` 实际运行（5s 初始延迟），测试在首次 poll 前完成
- `findByText` 在 badge 和时间线有重复文本，改用 `findAllByText` + `.not.toHaveLength(0)`

---

## 4. ReviewSubmitForm 未捕获提交流程异常

**现象**：API 500 错误测试产生 `Unhandled Rejection`，Vitest 报 error。

**根因**：`useReviewSubmission.submit()` 在 catch 块中 `setError()` 后 re-throw，但 `ReviewSubmitForm.handleSubmit()` 没有 try/catch。

**修复**：在 `handleSubmit()` 的 `await submit()` 外包 try/catch，catch 块为空（error 状态已由 hook 设置）。

---

## 5. Python 异步消费者和回调客户端已删除

**现象**：CLAUDE.md 和 README 提到 `mq/consumer.py`、`java_callback_client.py`、`POST /ai/review/async` 端点，但源码中不存在。

**澄清**：这是有意为之。当前架构中 Java 是编排层（发布 MQ + 消费 + 调 Python），Python 仅作为同步计算服务响应 HTTP 请求。Python 侧的 MQ 消费者是废弃的旧架构方案，已被删除。

---

## 6. Docker Desktop + WSL Bash 路径转换问题

**现象**：`docker exec my-kafka /opt/kafka/bin/kafka-topics.sh` 报错路径被自动拼接为 `D:/Git/opt/kafka/bin/...`。

**修复**：使用 `docker exec my-kafka bash -c "命令"` 包装，避免 Docker CLI 对路径的错误转换。

---

## 7. curl 发送中文 JSON 编码错误

**现象**：`curl -d` 发送含中文的 JSON 时 Java 报 `Invalid UTF-8 middle byte`。

**修复**：测试时使用全英文 payload，或在脚本中确保 UTF-8 编码正确。

---

## 8. 项目缺少 Maven Wrapper

**现象**：`./mvnw` 不存在，无法用 wrapper 启动。

**解决**：改用系统安装的 `mvn` 命令直接启动 `mvn spring-boot:run`。

---

## 9. Windows 进程管理

**现象**：WSL bash 中 `kill -9 PID` 无法终止 Windows 侧 Java 进程，`taskkill` 在 bash 中路径解析异常。

**解决**：用 `powershell -Command "Stop-Process -Id PID -Force"` 桥接终止进程。

---

---

## 10. 前端请求超时（"网络异常或请求超时"）

**现象**：前端提交审查后提示"网络异常或请求超时"，但后端日志显示 200 OK，请求实际耗时 40+ 秒。

**根因**：`frontend/src/api/client.ts` 中 AbortController 默认超时 15 秒（`VITE_HTTP_TIMEOUT_MS ?? 15000`），Python 需要 40-50 秒完成 LLM 调用链，15 秒一到直接 abort，走到 catch 块抛出 `ApiError({ status: 0, message: '网络异常或请求超时' })`。

**修复**：

| 文件 | 变更 |
|------|------|
| `client.ts` | 默认超时 15000 → 120000 |
| `vite.config.ts` | proxy 添加 `timeout: 120000` |
| `.env` | 添加 `VITE_HTTP_TIMEOUT_MS=120000` |

---

## 11. 日志查询返回 500

**现象**：任务详情页 `GET /api/review/logs/{taskId}` 返回 `500 No static resource api/review/logs/...`。

**根因**：Java 后端没有 `/api/review/logs/{taskId}` 路由。日志端点只在 Python 层（`/ai/review/logs/{task_id}`），前端直接调 Java 路径导致 404 fallback 到静态资源报错。

**修复**：在 Java 层加日志代理。涉及 4 个文件：
- `application.yml`：添加 `python.logs-path: /ai/review/logs`
- `PythonClientProperties.java`：添加 `logsPath` 字段
- `PythonComputeClient.java`：添加 `fetchLogs(String taskId)` GET 转发
- `ReviewController.java`：添加 `@GetMapping("/logs/{taskId}")`

---

## 12. 日志查询始终返回空数组

**现象**：修复 #11 后，`GET /api/review/logs/{taskId}` 返回 `[]`，Python 端实际返回 404 `Logs not found`。

**根因**：Java 传给 Python 的 `taskId` 在 Python 端被丢弃。`BackendSyncRequest` 模型没有 `taskId` 字段，导致 `to_review_request()` 创建的 `ReviewRequest` 使用 `default_factory=uuid4` 生成新的 UUID。日志按 Python 自生成 ID 存储，前端按 Java ID 查询 → 永远不命中。

**修复**：`python/schemas/backend_contract.py` 中 `BackendSyncRequest` 添加 `taskId: UUID | None = None`，`to_review_request()` 有值则透传，无值才走默认。

**效果**：日志数 0 → 7（`diff → classifier → impact → rules → rag → scoring → report`）。

---

## 13. 任务列表只显示"当前会话任务"

**现象**：`/tasks` 页面始终显示"0 个任务"，标题"当前会话任务"，看不到历史任务。

**根因**：`TaskDashboardPage` 只读取 Zustand 本地 store（`useTaskStore`），该 store 仅在表单提交时写入，从未调用服务端 `GET /api/review/tasks`。

**修复**：
- `api/task.ts`：新增 `fetchTaskList()` 函数
- `pages/TaskDashboardPage.tsx`：`useEffect` 加载时调接口，合并到 store，标题改为"所有已提交的审查任务"

---

## 14. Python 依赖缺失（networkx 等）

**现象**：`uv sync` 成功但启动报 `ModuleNotFoundError: No module named 'networkx'`。

**说明**：`uv sync` 检查了 92 个包但未安装缺失项。需用 `uv pip install networkx` 手动补装，或升级 `pyproject.toml` 中的依赖声明。

---

## 15. Python 日志格式 KeyError: trace_id

**现象**：健康检查等后台线程日志出现 `KeyError: 'trace_id'`，伴随大量 traceback。

**根因**：`config/logging.py` 的 `LOG_FORMAT` 使用 `%(trace_id)s`，但 ThreadPoolExecutor 中的日志记录未设置此属性。

**修复**：添加 `_DefaultTraceFilter`，对缺少 `trace_id`/`task_id` 的记录填充默认值 `-`。

---

## 16. MinIO 健康检查阻塞

**现象**：Python 健康检查耗时 >10s，日志大量 urllib3 `Retrying` 输出。

**根因**：`get_minio_client()` 使用 urllib3 默认重试策略（5 次指数退避），MinIO 不可用时每次健康探测阻塞数十秒。

**修复**：`repositories/db.py` 中 MinIO 客户端设置 `retries=Retry(total=0)` + `timeout=3`，3 秒即返回 DOWN。

---

---

## 17. 任务列表无分页

**现象**：`/tasks` 页面一次性加载全部任务（24 条），无翻页控件。

**修复**：
- 后端新增 `TaskListResponse` DTO，返回 `items` + `total` + `page` + `size` + `totalPages`
- `ReviewService.listTasks()` 改用 `selectCount` + `LIMIT offset, size` 手动分页（MyBatis-Plus `PaginationInnerInterceptor` 缺依赖无法使用）
- 前端 `fetchTaskList` 改为返回 `TaskListResponse`，新增 `page`/`size` 参数
- `TaskDashboardPage` 添加分页状态 + 上一页/下一页按钮，每页 5 条，翻页用 `setItems` 替换（不累积）

**相关文件**：`backend/.../dto/TaskListResponse.java`、`backend/.../service/ReviewService.java`、`backend/.../controller/TaskController.java`、`frontend/src/api/task.ts`、`frontend/src/pages/TaskDashboardPage.tsx`、`frontend/src/App.css`

---

## 18. 任务列表缺少按项目 ID 筛选

**现象**：无法按项目 ID 查询任务。

**修复**：
- `TaskDashboardPage` 添加搜索输入框 + 查询按钮 + 清除按钮
- 输入支持回车触发查询
- 筛选模式下隐藏分页（全量展示匹配结果）
- 空结果显示友好提示 "没有找到项目 xxx 的任务。"
- 后端已有 `?projectId=` 参数，前端直接透传

**相关文件**：`frontend/src/pages/TaskDashboardPage.tsx`、`frontend/src/App.css`

---

## 19. 提交页面显示 Trace ID

**现象**：提交审查页面底部显示 "Trace ID: xxx-xxx-xxx"。

**根因**：`useReviewSubmission` hook 中生成 `traceId` 用于 `X-Trace-Id` 请求头，`ReviewSubmitForm` 将其渲染到 UI。Trace ID 是内部调试信息，不应暴露给终端用户。

**修复**：删除 `<p className="trace-text">Trace ID: {traceId}</p>`，`traceId` 仍在后台通过请求头发送。

**相关文件**：`frontend/src/components/ReviewSubmitForm.tsx`

---

## 20. 任务详情页 result 不显示

**现象**：从任务列表点击进入详情页后，只显示执行进度时间线，不显示审查结果卡片。

**根因**：`TaskDashboardPage` 调用 `fetchTaskList` 时已将 task 写入 Zustand store。进入详情页时 `TaskDetailPage` 判断 `if (!task)` 为 false，跳过 `fetchTask` 调用 → result 永远为空。

**修复**：去掉 `!task` 条件，每次进入详情页都调用 `fetchTask` 获取完整数据（含 result），store 中的 task 作为 fallback 保留本地字段。

**相关文件**：`frontend/src/pages/TaskDetailPage.tsx`

---

## 21. 任务详情页 React 崩溃（Objects are not valid as a React child）

**现象**：访问任务详情页时 React 白屏报错 `Objects are not valid as a React child (found: object with keys {task_id, request})`。

**根因**：Python 日志的 `input`/`output` 字段是完整对象（如 `{"task_id":"...", "request":{...}}`），前端 `BackendReviewLogEntry` 类型声明为 `string`，但运行时 TypeScript 不做检查。React 收到对象后无法渲染，直接崩溃。

**修复**：`api/logs.ts` 新增 `toString()` 函数，将 `input`/`output`/`message` 统一转为字符串——字符串直接返回，对象 `JSON.stringify()`，`null`/`undefined` 返回空串。`BackendReviewLogEntry` 接口的字段类型改为 `unknown`。

**相关文件**：`frontend/src/api/logs.ts`

---

## 验证方式

```bash
# 前端测试
pnpm --dir frontend test:ci   # lint + test:run + build

# 异步接口端到端
curl -s -X POST http://localhost:8080/api/review/async \
  -H "Content-Type: application/json" \
  -H "X-API-Key: dev-key" \
  -d '{"projectId":"demo","projectName":"Demo","prUrl":"https://example.com/pr/1","diffContent":"diff --git ...","mode":"ASYNC"}'

# 轮询任务状态
curl -s http://localhost:8080/api/review/tasks/{taskId} -H "X-API-Key: dev-key"

# 重试卡住的 PENDING 任务
curl -s -X POST http://localhost:8080/api/review/tasks/{taskId}/retry -H "X-API-Key: dev-key"

```