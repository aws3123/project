# =============================================================================
# config 包初始化文件
# =============================================================================
# 作用：
#   当其他代码写 "from config import AppSettings" 时，Python 会先执行这个文件。
#   这里我们把 AppSettings（应用配置类）暴露到包级别，方便外部直接导入。
#
# 举个例子：
#   没有这行 → 你必须写 "from config.settings import AppSettings"
#   有了这行 → 你可以简写为 "from config import AppSettings"
# =============================================================================
from .settings import AppSettings
