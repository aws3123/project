### 一、核心设计思路 1. 事件标识与追踪 （避免重复输出）
前端实现 ( useBusinessRiskSse.ts:103-127 )：

- 每个事件都携带唯一的 eventId
- 使用 seenEventIds Set 记录已处理的事件ID
- 本地存储 lastEventId 到 localStorage，持久化断点位置
后端实现 ( BusinessRiskSseController.java:22-38 )：

- 每次重连时，前端携带 lastEventId 参数
- 后端先重播该 ID 之后的历史事件，再切换到实时流 2. 断点恢复流程 （恢复上下文）
完整的重连时序：
 3. 事件存储机制 （防止丢包）
后端存储策略 ( BusinessRiskEventStore.java )：

特性 实现方式 双层存储 内存 + Redis 双写，Redis 作为持久化备份 过期清理 按 TTL（默认 3600 秒）自动清理过期事件 数量限制 单会话最大 5000 条，全局最大 50000 条 查询优化 按 sessionId 分组，支持按 lastEventId 切片

关键代码 ( BusinessRiskEventStore.java:84-103 )：

### 二、去重与防丢包的关键机制 1. 去重策略 （三层保障）
层级 位置 机制 作用 L1 前端内存 seenEventIds Set 本次连接去重 L2 前端持久化 localStorage lastEventId 跨连接去重 L3 后端存储 按 ID 切片重播 服务端级别去重

前端去重逻辑 ( useBusinessRiskSse.ts:129-134 )：
 2. 防丢包策略
- 事件持久化 ：内存 + Redis 双写，确保服务重启不丢数据
- 重播机制 ：重连时根据 lastEventId 补传遗漏事件
- 心跳检测 ：定期发送 heartbeat 事件，检测连接状态
### 三、重连策略优化
指数退避重连 ( useBusinessRiskSse.ts:195-206 )：

```
const scheduleReconnect = () => {
    const delay = Math.min(RECONNECT_BASE_DELAY_MS * 2 ** reconnectAttempt, 
    RECONNECT_MAX_DELAY_MS)
    reconnectAttempt += 1
    const jitter = Math.floor(Math.random() * 250)  // 抖动避免惊群
    reconnectTimer = setTimeout(() => {
        reconnectTimer = undefined
        connect()
    }, delay + jitter)
}
```
重连次数 延迟（毫秒） 1 1000-1250 2 2000-2250 3 4000-4250 4 8000-8250 5+ 15000-15250（上限）

### 四、架构优势总结
维度 实现效果 可靠性 断点续传 + 双重存储，确保不丢消息 一致性 严格按事件顺序重播，保证数据完整性 性能 内存优先，Redis 兜底，兼顾速度与持久化 扩展性 支持水平扩展，通过 sessionId 分片 健壮性 指数退避 + 抖动，避免重连风暴

这套方案完整解决了流式返回中网络中断的三大问题： 上下文恢复 、 重复输出 、 消息丢包 ，是生产级 SSE 实现的最佳实践