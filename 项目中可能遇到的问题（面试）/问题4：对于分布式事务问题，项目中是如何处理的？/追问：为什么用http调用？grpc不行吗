基于对代码的分析，项目使用HTTP而不是gRPC主要有以下几个原因：

## 1. 技术栈匹配
- Python服务端 ：使用的是 FastAPI （见 main.py:46 ），这是一个专为HTTP REST API设计的框架
- Java客户端 ：使用的是 Spring WebClient （见 PythonComputeClient.java:29-168 ），是Spring生态系统中标准的HTTP客户端
两者天然支持HTTP，无需额外引入新的依赖和框架。

## 2. 简单的请求-响应模式
当前的通信模式非常简单：

- 单次请求 → 单次响应
- 没有流式传输
- 没有双向通信
- 请求频率不高
这种场景下，HTTP完全够用，gRPC的优势（流式、多路复用等）体现不出来。

## 3. 调试和测试友好
HTTP的优势：

- ✅ 可以用浏览器、curl、Postman直接测试
- ✅ 请求/响应是明文JSON，易于调试
- ✅ 网络抓包简单直接
- ✅ 现有监控和日志工具都支持HTTP
从代码中可以看到，项目使用了JSON格式进行数据交换（见 BusinessRiskPythonClient.java:78 ）。

## 4. 无需定义.proto文件
gRPC需要：

- 编写.proto文件定义接口
- 编译生成客户端和服务端代码
- 维护跨语言的代码生成流程
对于这个简单的项目，这会增加不必要的复杂度。

## 5. 性能足够
虽然gRPC通常比HTTP快，但对于这个场景：

- AI计算本身就需要较长时间（几秒到几分钟）
- 网络传输的开销占比很小
- HTTP的性能已经完全满足需求
代码中可以看到，项目有超时控制（见 WebClientConfig.java:26 ），说明主要瓶颈在AI计算，不在网络传输。

## 6. 生态系统成熟
Spring Boot和FastAPI在HTTP方面都有：

- 完善的错误处理
- 自动的序列化/反序列化
- 丰富的中间件支持（如日志、追踪、认证）
- 广泛的文档和社区支持
## 7. 现有功能支持
从代码中可以看到，项目已经利用HTTP实现了：

- 重试机制 ：见 BusinessRiskPythonClient.java:37-69 ，有完善的重试逻辑
- 超时控制 ：见 WebClientConfig.java:24-30
- Trace ID传递 ：见 PythonComputeClient.java:96 ，通过HTTP头传递追踪信息
- 服务发现 ：见 PythonComputeClient.java:61-67 ，支持动态实例选择
## 总结
特性 HTTP + JSON gRPC 本项目适用性 实现复杂度 ⭐ 简单 ⭐⭐⭐ 复杂 ✅ HTTP更好 调试方便度 ⭐⭐⭐ 很方便 ⭐⭐ 一般 ✅ HTTP更好 性能 ⭐⭐ 不错 ⭐⭐⭐ 很好 ⚠️ 差异不大 生态支持 ⭐⭐⭐ 成熟 ⭐⭐ 良好 ✅ HTTP更好 流式支持 ⭐ 无 ⭐⭐⭐ 原生 ❌ 不需要

结论 ：对于这个 简单的请求-响应、AI计算密集型、需要易调试 的场景，HTTP + JSON是最合适的选择。gRPC虽好，但在这里属于过度设计。