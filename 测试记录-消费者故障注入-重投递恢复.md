# 消费者故障注入与 Kafka 重投递恢复测试记录

## 1 测试概况

| 项目 | 内容 |
|---|---|
| 测试类型 | 消费者实例强制故障注入、Kafka 重投递恢复 |
| RunID | `dedup-fix-202609170305` |
| 项目ID | `perf-dedup-fix-202609170305` |
| 测试场景 | `soak`，恒定到达率 |
| 注入流量 | 10 req/s，持续45秒 |
| 计划任务数 | 450 |
| LLM模式 | mock，`LLM_MOCK_DELAY_SECONDS=2` |
| RAG模式 | 本地离线 RAG，`HF_HUB_OFFLINE=1`、`TRANSFORMERS_OFFLINE=1` |
| 故障对象 | Python 消费者实例 8000 |
| 故障方式 | 任务处理期间发送 `SIGKILL`，模拟进程瞬时崩溃 |

## 2 基准背景文档

本次测试依据和背景说明位于：

- `Kafka异步链路MQ化改造验收清单.md`
- `backend/k6/README_RESILIENCE.md`
- `测试记录-第10轮-3000任务.md`
- `Kafka重投递被Redis去重吞掉的故障恢复Bug.md`

## 3 测试前基线

### 3.1 部署拓扑

| 参数 | 基线值 |
|---|---|
| Kafka | 单机3逻辑Broker |
| `ai.review.tasks` | 4分区，副本因子3，`min.insync.replicas=2` |
| `ai.review.callbacks` | 2分区，副本因子3，`min.insync.replicas=2` |
| Java实例 | 1个 |
| Python实例 | 2个，端口8000/8001 |
| Python消费者组 | `python-review-worker` |
| Mock延迟 | 2秒 |

### 3.2 本次修复内容

旧逻辑在任务刚开始处理时写入 Redis `SETNX review:consumed:<taskId> = 1`。消费者进程被杀死后，Kafka offset 尚未提交，但重投消息会因 Redis 键仍存在而被跳过，任务最终被对账任务标记为超时失败。

本次改为处理中租约与完成态分离：处理中写入15秒租约并持续续租；只有 `RESULT` 或 `DEAD_LETTER` 回调成功后才写入长期完成态。批内任一任务处理异常时不提交 Kafka 批次，避免异常消息被错误确认。

## 4 测试脚本、日志和采集文件

### 4.1 测试脚本

- 压测脚本：`backend/k6/scenarios/s2_resilience.js`
- 故障注入脚本：`backend/k6/tools/chaos_kill_single_host.sh`
- 对账工具：`backend/k6/tools/verify_async_run.py`
- 消费者去重回归测试：`python/tests/mq/test_review_consumer_dedup.py`

### 4.2 服务器文件

- k6日志：`/workspace/dedup-fix-202609170305-k6.log`
- Python实例日志：`/workspace/python/instance-8000.log`、`/workspace/python/instance-8001.log`
- 修复前版本备份：`/workspace/backups/review-consumer-20260916185737`

## 5 测试过程数据

### 5.1 k6受理结果

| 指标 | 结果值 |
|---|---:|
| 提交任务数 | 450 |
| HTTP失败率 | 0.000% |
| 受理失败数 | 0 |
| 目标到达率 | 10 req/s |
| 持续时间 | 45秒 |

### 5.2 故障注入与恢复

| 指标 | 结果值 |
|---|---:|
| 故障实例 | Python 8000 |
| 故障信号 | `SIGKILL` |
| 故障时状态 | 任务持续处理中 |
| 处理租约 | 15秒，任务执行期间续租 |
| 重启后实例数 | 2 |
| 消费者组分配 | 4个任务分区均恢复分配 |
| 故障后新增丢失任务 | 0 |

## 6 测试结束结果

对账时间：`2026-09-16T19:00:11.800856Z`。

| 结束指标 | 结果值 |
|---|---:|
| 总任务数 | 450 |
| 成功任务数 | 450 |
| 失败任务数 | 0 |
| 终态任务数 | 450 |
| Outbox记录数 | 450 |
| Outbox pending | 0 |
| 终态无结果任务 | 0 |
| 重复结果任务 | 0 |
| Outbox/终态对账差异 | 0 |
| task Kafka lag | 0 |
| Python实例数 | 2 |

服务器完整 Python 环境中的 Redis 去重回归测试结果为 `3 passed in 0.07s`。

## 7 结论与限制

本轮证明：消费者在处理任务期间发生瞬时崩溃时，未提交的 Kafka 消息能够重新投递；新的租约机制不会把重投消息错误当作重复消息丢弃。450个任务最终全部成功，失败数、重复结果数、Outbox差异和 Kafka lag 均为0。

本轮只验证了单个 Python 消费者故障，不代表 Kafka Broker 故障容灾结论；Broker 故障、SSE断线和长时间稳态浸泡仍需单独记录。故障注入窗口未单独持久化精确 rebalance 起止时间，因此本记录不把滚动重启时的7.410秒和7.352秒恢复值冒充本轮故障窗口指标。
